import { StyleSheet } from "react-native";

export default StyleSheet.create({
  main: { flex: 1, justifyContent: "center", alignItems: "center" },
  label: { fontSize: 16, fontWeight: "700", color: "black" },
});
