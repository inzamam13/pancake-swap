import React from "react";
import { SafeAreaView, Text, View } from "react-native";

export default function (props) {
  return (
    <SafeAreaView>
      <Text>{`Find Price `}</Text>
    </SafeAreaView>
  );
}
